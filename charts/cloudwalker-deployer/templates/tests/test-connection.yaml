apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paved-road-service.fullname" . }}-test-connection"
  labels:
    {{- include "paved-road-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paved-road-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
